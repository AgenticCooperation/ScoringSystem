rule:
  id: "dns_over_tls"
  name: "DNS over TLS"
  description: "DNS over TLS özelliğinin etkin olup olmadığını kontrol eder"
  category: "network_security"
  severity: "medium"
  weight: 6
  
check:
  field: "network.dns.dns_over_tls"
  operator: "equals"
  expected_value: true
  
message:
  pass: "DNS over TLS etkin"
  fail: "DNS over TLS devre dışı - DNS güvenliği zayıf!"
  
remediation:
  action: "enable_dns_over_tls"
  script: "scripts/enable_dns_over_tls.ps1"
  description: "DNS over TLS'yi etkinleştir"
